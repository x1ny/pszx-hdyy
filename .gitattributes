# 文本在索引和工作目录中都使用 LF，覆盖个人 core.autocrlf 设置。
# text=auto 让 Git 识别二进制；下面再显式保护仓库已有的二进制资源。
* text=auto eol=lf

# Windows 批处理保留 CRLF；不改变其他源码和脚本的换行约定。
*.bat text eol=crlf
*.cmd text eol=crlf

# Linux 入口和无扩展名 Git hook 必须保持 LF。
*.sh text eol=lf
.githooks/* text eol=lf

# 保留显式迁移约束：Drizzle hash 按字节计算，工作目录也必须与仓库一致。
apps/server/drizzle/**/*.sql text eol=lf

*.png binary
*.jpg binary
*.doc binary
*.docx binary
*.gz binary
