{
  "name": "@repo/server",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "dev": "bun --env-file=../../.env --hot run src/index.ts",
    "start": "bun --env-file=../../.env run src/index.ts",
    "auth:generate": "bun run @better-auth/cli generate --config src/lib/auth.ts --output ../../packages/db/src/auth-schema.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@repo/db": "workspace:*",
    "better-auth": "^1.6.26",
    "hono": "^4.13.0"
  },
  "devDependencies": {
    "@better-auth/cli": "^1.4.21",
    "@types/bun": "^1.3.14"
  }
}
