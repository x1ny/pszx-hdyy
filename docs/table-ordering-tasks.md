# 人工排序开发任务卡

> 2026-09-10：首个实际目标已确定为活动人员名单，使用 [活动人员名单执行方案](activity-member-ordering-model-plan.md) 的 AM00—AM05。下列 T01—T06 是原议程预选样例，均未实施，本次不按其中的 agenda 路径派发；T07 的抽取原则仍供真正的第二个消费者参考。

本文件保留原先面向议程线的 T01—T06 派发卡，供追溯设计取舍；它们没有执行，也不代表当前代码状态。当前实际实现请以 [活动人员名单执行方案](activity-member-ordering-model-plan.md) 的 AM00—AM05 和 [执行记录](activity-member-ordering-execution.md) 为准。单次派发一个任务 ID；任务通过验收后再派发下一项。

每个执行者先读 [table-ordering Skill](../.agents/skills/table-ordering/SKILL.md)、本任务卡和实施方案指定章节，无需读取原始聊天或整份研究报告。已有代码与实际任务状态必须核对，不能因为文档写了未来路径就认为该文件已存在。

## 执行顺序与状态

| 任务 | 交付物 | 前置 | 当前状态 |
| --- | --- | --- | --- |
| T01 | 纯排序计划函数与行为测试 | 无 | 未实施 |
| T02 | 隐藏列、历史回填、种子与完整读取顺序 | T01 | 未实施 |
| T03 | 议程线事务保存和移动 API | T02 | 未实施 |
| T04 | 数字编辑、按钮移动、保存和刷新 | T03 | 未实施 |
| T05 | 拖拽库接入与行交互适配 | T04 | 未实施 |
| T06 | 迁移、事务、浏览器的完整验收 | T05 | 未实施 |
| T07 | 第二张实际业务表接入并抽取共用代码 | T06＋确定的第二表任务 | 待具体业务任务 |

在原议程方案中，T01—T06 构成一个可使用样例；它们当前全部保留为未实施的历史任务。活动人员名单的首个实际样例不依赖这组 agenda 路径，T07 仍不属于首个样例完成的必要条件。

所有任务默认遵守根 `AGENTS.md`，只改变任务范围内文件。文件已被其他工作改动时保留现有修改，结合当前内容实施。必要的同模块类型调用方和回归测试可以同步修复，完成说明列出原因；不得把无关模块重构包含进来。

## T01

**实现纯排序计划函数。**

输入依据：实施方案第 4—5 节。本任务针对历史 agenda 样例；不访问数据库、不安装前端依赖。

新增文件：

- `apps/server/src/modules/agenda/plan-manual-order.ts`
- `apps/server/src/modules/agenda/plan-manual-order.test.ts`

实施步骤：

1. 按第 5 节确定接口实现 `planManualOrder`，支持 set 和 move 两种操作及明确错误结果。
2. 校验输入，复制后得到标准顺序；按 R03—R07 生成实际变化的更新项。不要改动原数组或原对象。
3. 将 A01—A12 写成行为测试；增加有限小序列枚举，使用独立的 ID 列表移动逻辑作为期望结果。
4. 断言数据库按三项键排序后的 ID 序列正确、其他可见值不变、非移动记录相对顺序不变，并检查无操作的 updates 为空。

检查命令，在仓库根执行：

```powershell
bun test ./apps/server/src/modules/agenda/plan-manual-order.test.ts
bun run --filter '@repo/server' typecheck
bunx biome check apps/server/src/modules/agenda/plan-manual-order.ts apps/server/src/modules/agenda/plan-manual-order.test.ts
```

完成条件：上述检查通过；固定用例、枚举规模和结果写入任务完成记录。文件不 import 数据库、Hono、业务 schema 或前端代码；此时不提前提升 shared。

派发提示词：

> 阅读 `.agents/skills/table-ordering/SKILL.md`，完成 `docs/table-ordering-tasks.md` 的 T01。按实施方案第 4—5 节实现确定接口与行为测试，只执行 T01。报告修改文件、用例覆盖、实际检查结果和剩余问题；不要开始数据库或 UI 开发。

## T02

**为议程线增加隐藏顺序，保持历史显示顺序。**

前置：T01 文件和测试已存在且通过。输入依据：实施方案第 4、6、9 节；现有数据库迁移文档。

主要文件：`modules/agenda/schema.ts`、`modules/agenda/routes.ts`、`dev-seed/30-agenda.ts`、本次生成的 `apps/server/drizzle/` 迁移与元数据、必要的 schema／查询测试。

实施步骤：

1. 添加隐藏 integer 列和普通查询索引；保持原 order、主线唯一约束、外键和字段投影。
2. 生成新迁移，在新迁移中加入第 6.1 节的回填 SQL。核对迁移只改变预期表，不修改历史迁移。
3. `/list` 的线路排序增加隐藏键；环节排序保持按时间。不要为客户端新增 sortIndex 写入参数。
4. 更新 typed seed，并增加 99、99、120 验收行。检查主线两处懒创建使用默认隐藏值 0 即可；无需改动其业务流程。
5. 在一次性库保留一组迁移前重复值快照，升级后比较可见值、ID 顺序和隐藏值；将结果交给 T06 复核。不可只用新库种子代替迁移验证。

检查命令：

```powershell
bun run db:generate
bun test ./apps/server/src/schema-drift.test.ts ./apps/server/src/modules/agenda/routes.test.ts
bun run --filter '@repo/server' typecheck
```

另对实际修改源码执行只读 Biome 检查；通过 `bun run dev` 验证种子和完整线路读取。上述环境命令不是对持久库迁移的授权。

完成条件：字段与迁移一致；I07 通过；原有议程层次和环节时间不变；记录迁移文件名、新增字段、初始化方式及验证数据。明确说明写入接口仍待 T03，不能把此阶段单独当完整功能交付。

派发提示词：

> 阅读 table-ordering Skill，核对 T01 已完成，再完成 T02。保持历史可见数字和顺序，生成新迁移并更新种子与读取；在本 worktree 一次性环境验证，不改持久数据。仅交付 T02，附迁移和检查证据。

## T03

**把手填值与移动意图落成完整事务。**

前置：T02 已完成。输入依据：实施方案第 4—7 节，特别是部分字段更新和邻居 updatedAt 的约束。

主要文件：`modules/agenda/line-order.ts`（新）、`validation.ts`、`routes.ts`、相应校验与行为测试。阅读 `shared/result.ts`、`infra/db.ts`、`auth/permission-map.ts` 的现有约定。

实施步骤：

1. 新增 moveLine 入参；源和锚点均检查存在性、activityId 与 parallel 类型。新路由注册在已有链上并受现有前缀权限保护。
2. 模块内事务读取完整并行线范围，映射到 `planManualOrder`，按 updates 写回实际变化项。调用方不提交隐藏编号。
3. `updateLine` 改成实际字段修改：遗漏名称／排序表示不改，名称显式 null 表示清空，保留主线和并行线不同校验。排序未提供时绝不默认写 0。
4. `createLine` 区分缺省 order 和显式 0，按 R11 分配尾部 order 和组内编号。删除仍不重编号。
5. 源行真实变更正常记审计；邻居仅隐藏更新显式保留审计列。验证在真实 Drizzle 写入中有效。
6. 所有预期拒绝发生在写入前，写入途中错误让事务回滚。返回值保持现有结果信封的精确推导。

新增分配按实施方案第 7.1 节的集中 helper 处理，不在多个路由内联复制。注意 I10：源行真实移动时，不能仅凭其排序字段是否有 patch 判断是否需要记录源操作。

自动测试至少覆盖：参数非法、遗漏字段和显式 null 的区别、同值不重排、主线限制、默认新增和显式 0。已有 SQL 检查可补三项排序与 scope 条件，但单独报告其证据范围。

检查命令：

```powershell
bun test ./apps/server/src/modules/agenda
bun test ./apps/server/src/modules/auth/permission-map.test.ts
bun run typecheck
```

另检查本次改动源码格式；在一次性环境实际请求 createLine、updateLine、moveLine 和 list，覆盖 I01—I04、I06、I09。中途失败回滚的最终实机复核归 T06。

完成条件：真实请求后刷新结果符合规范，跨活动不产生修改；名称单改不重写顺序，邻居时间戳不变。保留旧前端仍显式提交 sortOrder 的兼容行为，新前端防旧草稿覆盖由 T04 完成。

派发提示词：

> 核对 T02 后完成 T03。只在 agenda 内实现排序事务和接口，复用 T01 计划函数，按实施方案第 7 节处理字段遗漏、主线、scope 和审计信息。用真实请求证明保存有效，报告自动测试与数据库验证各自的结果。

## T04

**先完成可用的数字编辑和按钮移动。**

前置：T03 已完成。输入依据：实施方案第 4、6、8.1—8.2 节及 I02、I03、U02—U04、U06。

主要文件：现有 `features/agenda/queries.ts`、议程父页面、`-components/agenda-line-dialog.tsx`，以及此页面的相关测试。前端目录包含 `$`，PowerShell 读取和运行测试时使用单引号保护完整路径。

实施步骤：

1. 增加 moveAgendaLine API 函数，按接口推导请求类型；更新 create／update 参数表达，避免手抄一套不兼容的类型。
2. 输入保留字符串草稿，保存时校验，不把空串或错误数字转成 0。只发送实际修改字段；成功清除对应草稿，失败保留；提供取消行编辑。
3. 新增上移、下移入口，通过同一个 moveLine API；主线排除，首尾按钮正确禁用。
4. 任一已有行有未保存编辑时禁用移动；保存和刷新期间共享 pending，避免重复请求。
5. 成功 await 当前活动缓存刷新；区分保存失败、成功后刷新失败和超时结果不确定。暂不对全局缓存作乐观重排。
6. 新增时由服务端决定缺省尾部，删除前端“并行线数量＋1”的逻辑。

自动测试关注用户行为：按钮发出的锚点、首尾禁用、未保存草稿禁用、只改名称的请求不含 sortOrder、失败保留输入、pending 不重入。Mock API 可以验证前端请求，但不能声称它验证了数据库。

检查命令：

```powershell
bun run --filter '@repo/web' typecheck
bun run --filter '@repo/web' test 'src/routes/_authenticated/project/$projectId_.activity.$activityId.agenda'
```

另对改动源码执行 Biome 检查，浏览器实际完成两次按钮移动并刷新。此时不安装拖拽库，T05 将复用已有 onMove 保存链。

完成条件：用户仅用点击和数字输入即可精确调整同值顺序；名字编辑不会覆盖刚保存的排序；时间轴和重新打开的弹窗一致。

派发提示词：

> 在 T03 已完成的基础上执行 T04。接通数字输入、上移下移与保存刷新，保留现有 Table 和页面结构。严格按实际字段修改提交，测试草稿、pending 与失败恢复。先完成这一张表的按钮流程，只交付 T04。

## T05

**将拖拽接入已可用的移动流程。**

前置：T04 已完成，按钮可在真实环境保存。输入依据：实施方案第 3、4、8.3—8.4 节；当前 dnd-kit 官方 React 文档。UI 实现时按项目要求读取适用的 shadcn 指引。

主要文件：`apps/web/package.json`、实际 Bun 锁文件、议程页 `-hooks/use-manual-row-dnd.ts` 与 `-components/manual-sortable-row.tsx`、管理弹窗、相应测试。只创建确实被本页面使用的文件。

实施步骤：

1. 核对项目当前依赖，选择当前官方 React 文档对应版本，在 `apps/web` 用 Bun 安装明确版本。默认候选为新线 `@dnd-kit/react`；不得混用旧线的包名、Provider 和事件模型。
2. 围绕现有 Table 接入 Provider、行 Hook、手柄和反馈；先验证长名称、输入控件、Dialog 内滚动及列宽，再决定细节样式。
3. 对业务层只输出稳定 ID 和前／后锚点。库里的渲染 index 不写回 sortIndex；取消、落点无效、最终序列未变时不调用保存。是否变化对比拖动开始时的序列，不对比已重排的临时数组。
4. 拖拽与按钮共用 T04 的 onMove、pending、草稿限制和刷新机制，不能再创建第二条写入逻辑。
5. 保持合法表格 DOM、可读手柄名称和焦点；上移／下移入口保留。
6. 记录实际安装版本、import 路径、通过验证的行组件与 Hook 路径，作为后续接入的来源。

检查命令与 T04 相同，另运行 `bun run build` 并检查改动源码。UI 自动测试可检查适配事件和禁用条件，但 U01、U05、U06 必须通过真实浏览器验证。

完成条件：99 中间插入、跨值移动、取消、无变化、按钮与拖拽交替操作均正确；没有明显列宽跳变或输入误触；版本记录有实际安装和运行依据。库接入若出现无法解决的兼容问题，记录复现与版本，不随意切换到另一套库继续扩大任务。

派发提示词：

> 核对 T04 后执行 T05，用 dnd-kit 为现有议程线表格加入拖拽。复用已有按钮移动与保存链，固定实际验证的包版本，隔离第三方事件。完成真实表格验证并更新依赖基线，只交付 T05。

## T06

**验收首个完整样例并形成可复用基线。**

前置：T01—T05 已完成。输入依据：实施方案第 9 节全部用例。

允许在 agenda 及其前端范围修复验收发现的问题并增加对应回归测试。不得借验收改其他模块的产品规则或重新设计排序模型。

实施步骤：

1. 复核 A01—A12 自动测试与每项 I／U 用例证据，补做未覆盖项。
2. 在一次性数据库验证历史回填，以及一次真实保存中后续写入失败时所有前序更新回滚。使用临时测试约束、测试事务包装或本地故障注入；不得加入生产 API 失败开关。检查确实发生了前序写入再失败。
3. 浏览器验证完整操作序列：输入 99 → 同值拖动 → 改名称 → 按钮移动 → 取消拖动 → 刷新／重新打开 → 时间轴顺序。
4. 检查隐藏重编号没有改变邻居审计信息，主线和环节时间没有变化，未移动记录相对顺序保持。
5. 核对自动测试、数据库与浏览器三类证据分别真实，不把 Mock 或 happy-dom 结果冒充实机通过。
6. 运行完整收尾检查，更新下方“实施基线”和任务状态。已经通过的检查仅在后续修改影响它时重跑。

完整检查：

```powershell
bun run typecheck
bun run test
bun run build
git diff --check
```

对本次修改源码另执行 `bunx biome check <实际路径>`。按根约定精确记录现存已知失败，不修改无关测试。环境无法完成的验证必须列为未通过验收条件，不能将 T06 标记完成。

完成条件：自动检查和全部适用 I／U 用例通过，或明确列出仍未满足的条件并保持未完成。依赖、样例和接口都能通过下方基线找到。应用实现完成不等于获准部署或合并。

派发提示词：

> 执行 T06，验收 T01—T05 的议程人工排序。按实施方案第 9 节核对自动、数据库、浏览器证据；在一次性环境验证迁移和真实回滚，修复范围内问题。更新任务状态和实施基线，报告可以确认的结果与仍未验证项。

## T07

**为第二个实际业务使用方抽取共用能力。**

这是后续任务的派发模板，不代表已选定或授权修改任何第二张表。T06 完成后，由具体业务需求提供下面的绑定表；字段不足时先完成范围调查，不能默认为全局排序或随意选择一张表凑复用数。

| 必须明确的绑定 | 需要填写的事实 |
| --- | --- |
| 目标对象和入口 | 哪张表、哪条页面路由、哪一个操作对象 ID |
| 真实范围 | 全局、projectId、activityId、某关系表，或其他确定条件 |
| 可见 order | 已有字段或新字段；合法范围、默认追加行为 |
| 固定和隐藏行 | 哪些行不能移动；是否存在权限隐藏、停用项或分组 |
| 读取方式 | 分页／全量、默认排序、筛选生效状态、完整范围来源 |
| 接口和权限 | 现有读写路径与权限点，新增动作路径 |
| 所有写入口 | 新增、编辑、导入、复制、恢复及批量操作 |
| 所有读出口 | 列表、导出、打印、H5 等实际存在的出口 |
| 历史初始化 | 保持哪一套旧 ID 顺序；旧空值、重复值如何回填 |
| 首次样例基线 | 已通过的函数、UI 适配、版本和检查证据位置 |

建议按三个小任务顺序派发，不让低成本模型一次处理全部迁移：

1. **T07-A，共用纯逻辑与第二模块后端。** 将算法和测试搬到 shared，删除原实现并修正 agenda 引用；如果新增尾部编号分配也重复，抽取无 I/O 的公共函数。第二模块保留自己的数据库与权限编排。完成两个模块后端回归，证明只有一份正式算法。
2. **T07-B，共用交互与第二页面接入。** 在两个真实用例基础上提升不认识业务的 Hook／行适配，两个页面直接引用；API、缓存和业务列继续本地维护。更新基线中的最终路径，不保留跨路由私有引用或复制版事件解析。
3. **T07-C，第二表完整验收。** 按实际分页、筛选和读写出口补用例，跑第一样例回归，确认通用代码改变未破坏 agenda。

如果第二表的固定行、树形或数值含义与既有契约冲突，提交最小反例进行规则评估，再派发新任务；不要临时增加 `mode`、`strategy` 或多个布尔开关让公共 API 承担尚未确认的业务。

派发提示词模板：

> 阅读 table-ordering Skill，执行 T07-A／B／C 中本次指定的一项。目标业务绑定如下：〔附已填写绑定表〕。使用已验证基线，按实施方案第 3 节提升真实共用部分，并维护 agenda 回归。不要复制排序算法，不更换拖拽库，不扩大业务范围。

## 实施基线

以下为当前代码与验收的事实基线；T01—T06 仍仅是未实施的历史 agenda 任务，不能用来推断 agenda 代码已经存在。

| 项目 | 当前记录 |
| --- | --- |
| 首个样例 | 活动管理 → 人员名单；已实现，事实见活动人员名单执行方案 |
| 纯算法实际路径 | `apps/server/src/modules/member/plan-manual-order.ts`；议程 T01 仍未实施 |
| 模块事务实际路径 | `apps/server/src/modules/member/activity-member-ordering.ts`；议程 T03 仍未实施 |
| 拖拽包与精确版本 | `@dnd-kit/react@0.5.0`，已验证；议程 T05 仍未实施 |
| 拖拽 API 文档 | 候选为 `https://dndkit.com/react/` 当前文档；T05 记录与版本匹配的具体页面 |
| 行组件／Hook 实际路径 | `apps/web/src/routes/_authenticated/project/$projectId_.activity.$activityId.members/-components/activity-member-ordering.tsx` |
| 迁移文件与实机验证 | `apps/server/drizzle/0007_mature_post.sql`、`0008_lame_maelstrom.sql`；实机证据见活动人员名单执行记录 |
| I／U 用例与完整检查 | 已执行；服务端 member 118 项、人员页 10 项、构建通过；已知基线失败见执行记录 |
| 第二使用方 | 尚未指定，不执行 T07 |

## 每个任务的完成记录

执行者在本节追加自己的结果；引用文件和证据即可，不粘贴长日志或完整实现。未执行的任务保持上方状态不变。

```text
任务：Txx
结果：完成／未完成
实际改动：文件路径＋对应职责
规则覆盖：本次涉及的 R／A／I／U 编号
检查：实际命令、结果；已有失败单独列出
数据库／浏览器：适用场景、实际结果、证据路径；未执行明确说明
偏差：与计划不一致之处、原因和影响；没有则写无
交接：下一个任务已满足的前置条件及尚未解决事项
```

低成本模型的实际开发效果应在后续任务中记录，尤其是首次通过情况、返工次数、规则误解和重复实现。当前完成的是可派发方案，不代表已经完成这些运行验证。
